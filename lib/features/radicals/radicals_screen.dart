import 'package:flutter/material.dart';

class RadicalsScreen extends StatelessWidget {
  const RadicalsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
